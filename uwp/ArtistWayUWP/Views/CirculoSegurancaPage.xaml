<Page
    x:Class="ArtistWayUWP.Views.CirculoSegurancaPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArtistWayUWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="16,16,16,24">
        <StackPanel>
            <TextBlock Text="Círculo de Segurança" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock Text="Quem apoia -- e de quem se proteger por enquanto. Toque num nome pra mover de lado." Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" TextWrapping="Wrap" Margin="0,4,0,16" />

            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Nome" Style="{StaticResource BodyTextBlockStyle}" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <TextBox x:Name="NameBox" />
                    <Button Content="Adicionar em &quot;Apoia&quot;" Click="AddSafe_Click" HorizontalAlignment="Stretch" Margin="0,12,0,0" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Apoia" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <StackPanel x:Name="SafePanel" Margin="0,8,0,0" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Cautela" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <StackPanel x:Name="CautionPanel" Margin="0,8,0,0" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
