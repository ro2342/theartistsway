<Page
    x:Class="ArtistWayUWP.Views.WeekDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArtistWayUWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="16,16,16,24">
        <StackPanel>
            <TextBlock x:Name="HeaderText" Style="{StaticResource TitleTextBlockStyle}" TextWrapping="Wrap" />
            <TextBlock x:Name="IntroText" Style="{StaticResource BodyTextBlockStyle}" Opacity="0.85" TextWrapping="Wrap" Margin="0,8,0,0" />
            <Button x:Name="EssayButton" Content="Entenda o tema da semana" Click="OpenEssay_Click" HorizontalAlignment="Stretch" Margin="0,12,0,16" />
            <StackPanel x:Name="ChecklistPanel" />
            <Button Content="Fazer o check-in dessa semana" Click="OpenCheckin_Click" HorizontalAlignment="Stretch" Margin="0,16,0,0" />
        </StackPanel>
    </ScrollViewer>
</Page>
