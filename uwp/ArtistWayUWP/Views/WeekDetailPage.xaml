<Page
    x:Class="ArtistWayUWP.Views.WeekDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArtistWayUWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="16,16,16,24">
        <StackPanel>
            <TextBlock x:Name="HeaderText" Style="{StaticResource TitleTextBlockStyle}" TextWrapping="Wrap" />
            <TextBlock x:Name="IntroText" Style="{StaticResource BodyTextBlockStyle}" Opacity="0.85" TextWrapping="Wrap" Margin="0,8,0,0" />
            <Button x:Name="EssayButton" Click="OpenEssay_Click" HorizontalAlignment="Stretch" Margin="0,12,0,16">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <SymbolIcon Symbol="Library" />
                    <TextBlock Text="Entenda o tema da semana" Margin="8,0,0,0" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            <StackPanel x:Name="ChecklistPanel" />
            <Button Click="OpenCheckin_Click" HorizontalAlignment="Stretch" Margin="0,16,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <SymbolIcon Symbol="Accept" />
                    <TextBlock Text="Fazer o check-in dessa semana" Margin="8,0,0,0" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </StackPanel>
    </ScrollViewer>
</Page>
