<Page
    x:Class="ArtistWayUWP.Views.ProfilePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArtistWayUWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="16,16,16,24">
        <StackPanel>
            <TextBlock x:Name="NameLabelText" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" Margin="0,0,0,4" />
            <TextBox x:Name="NameBox" />

            <TextBlock x:Name="StartDateLabelText" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" Margin="0,12,0,4" />
            <DatePicker x:Name="StartDatePicker" HorizontalAlignment="Stretch" />

            <TextBlock x:Name="MpTimeLabelText" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" Margin="0,12,0,4" />
            <TimePicker x:Name="MorningPagesTimePicker" HorizontalAlignment="Stretch" />

            <TextBlock x:Name="AdDayLabelText" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" Margin="0,12,0,4" />
            <ComboBox x:Name="ArtistDateDayCombo" HorizontalAlignment="Stretch" />

            <TextBlock x:Name="AdTimeLabelText" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" Margin="0,12,0,4" />
            <TimePicker x:Name="ArtistDateTimePicker" HorizontalAlignment="Stretch" />

            <TextBlock x:Name="CiDayLabelText" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" Margin="0,12,0,4" />
            <ComboBox x:Name="CheckinDayCombo" HorizontalAlignment="Stretch" />

            <TextBlock x:Name="CiTimeLabelText" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" Margin="0,12,0,4" />
            <TimePicker x:Name="CheckinTimePicker" HorizontalAlignment="Stretch" />

            <Button x:Name="SaveProfileButton" Click="SaveProfile_Click" HorizontalAlignment="Stretch" Margin="0,16,0,0" />

            <Border Style="{StaticResource SectionDividerStyle}" />

            <TextBlock x:Name="CalendarSectionTitleText" Style="{StaticResource SubtitleTextBlockStyle}" />
            <TextBlock x:Name="CalendarSectionDescriptionText" Style="{StaticResource BodyTextBlockStyle}" Opacity="0.85" TextWrapping="Wrap" Margin="0,4,0,12" />
            <Button x:Name="AddMpCalendarButton" Click="NativeCalMp_Click" HorizontalAlignment="Stretch" />
            <Button x:Name="AddAdCalendarButton" Click="NativeCalAd_Click" HorizontalAlignment="Stretch" Margin="0,8,0,0" />
            <Button x:Name="AddCiCalendarButton" Click="NativeCalCi_Click" HorizontalAlignment="Stretch" Margin="0,8,0,0" />
        </StackPanel>
    </ScrollViewer>
</Page>
