<Page
    x:Class="ArtistWayUWP.Views.ArtistDateHistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArtistWayUWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="16,16,16,24">
        <StackPanel>
            <TextBlock Text="Histórico de Artist Dates" Style="{StaticResource TitleTextBlockStyle}" TextWrapping="Wrap" />
            <TextBlock Text="todos os encontros já registrados, do mais recente pro mais antigo" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" TextWrapping="Wrap" Margin="0,4,0,16" />

            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel x:Name="HistoryPanel" />
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
