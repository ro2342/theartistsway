<Page
    x:Class="ArtistWayUWP.Views.QuizPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArtistWayUWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="16,16,16,24">
        <StackPanel>
            <TextBlock x:Name="TitleText" Style="{StaticResource TitleTextBlockStyle}" TextWrapping="Wrap" />
            <TextBlock x:Name="SubText" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" TextWrapping="Wrap" Margin="0,4,0,16" />

            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <StackPanel x:Name="QuestionsPanel" />
                    <Button Content="Ver resultado" Click="SeeResult_Click" HorizontalAlignment="Stretch" Margin="0,12,0,0" />
                    <TextBlock x:Name="ResultText" TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}" Margin="0,12,0,0" />
                </StackPanel>
            </Border>

            <Border x:Name="HistoryCard" Style="{StaticResource CardBorderStyle}" Visibility="Collapsed">
                <StackPanel>
                    <TextBlock Text="Tentativas anteriores" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <StackPanel x:Name="HistoryPanel" Margin="0,8,0,0" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
