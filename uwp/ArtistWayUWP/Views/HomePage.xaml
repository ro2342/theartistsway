<Page
    x:Class="ArtistWayUWP.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArtistWayUWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <!--
      Nota: sem StackPanel.Spacing / Grid.ColumnSpacing/RowSpacing em
      nenhuma página deste projeto — essas propriedades só existem a
      partir do Fall Creators Update (10.0.16299), e o app declara suporte
      desde 10.0.14393 (Anniversary Update, a base real do Lumia 830).
      Espaçamento sempre via Margin, que funciona em qualquer versão.
    -->
    <Page.Resources>
        <Style x:Key="StreakDotStyle" TargetType="Border">
            <Setter Property="Width" Value="32" />
            <Setter Property="Height" Value="32" />
            <Setter Property="CornerRadius" Value="16" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="BorderBrush" Value="{ThemeResource SystemControlForegroundBaseLowBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Margin" Value="0,0,6,0" />
        </Style>
    </Page.Resources>

    <ScrollViewer Padding="16,16,16,24">
        <StackPanel>
            <TextBlock x:Name="GreetingText" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" Margin="0,0,0,16" />

            <Border x:Name="MaintenanceCard" Visibility="Collapsed" Style="{StaticResource NudgeBorderStyle}">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Modo manutenção" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" HorizontalAlignment="Center" />
                    <TextBlock Text="As 12 semanas terminaram — agora é só manter Morning Pages e Artist Date no seu ritmo." Style="{StaticResource BodyTextBlockStyle}" FontWeight="SemiBold" TextWrapping="Wrap" TextAlignment="Center" Margin="0,4,0,0" />
                </StackPanel>
            </Border>

            <Border x:Name="WeekDecisionCard" Visibility="Collapsed" Style="{StaticResource NudgeBorderStyle}">
                <StackPanel>
                    <TextBlock x:Name="WeekDecisionTitleText" Style="{StaticResource SubtitleTextBlockStyle}" TextWrapping="Wrap" />
                    <TextBlock x:Name="WeekDecisionSummaryText" Style="{StaticResource BodyTextBlockStyle}" TextWrapping="Wrap" Opacity="0.85" Margin="0,4,0,0" />
                    <Button x:Name="StayWeekButton" Click="StayWeek_Click" HorizontalAlignment="Stretch" Margin="0,12,0,0" />
                    <Button x:Name="AdvanceWeekButton" Click="AdvanceWeek_Click" HorizontalAlignment="Stretch" Margin="0,8,0,0" />
                </StackPanel>
            </Border>

            <Border x:Name="WeekCard" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock x:Name="WeekLabelText" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" />
                    <TextBlock x:Name="WeekTitleText" Style="{StaticResource SubtitleTextBlockStyle}" TextWrapping="Wrap" Margin="0,4,0,0" />
                    <TextBlock x:Name="WeekIntroText" Style="{StaticResource BodyTextBlockStyle}" TextWrapping="Wrap" Opacity="0.85" Margin="0,4,0,0" />
                    <ProgressBar x:Name="WeekProgressBar" Minimum="0" Maximum="100" Height="8" Margin="0,12,0,0" />
                    <TextBlock x:Name="WeekProgressLabel" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" Margin="0,4,0,0" />
                    <Button x:Name="OpenWeekButton" Content="Ver tarefas da semana" Click="OpenWeek_Click" HorizontalAlignment="Stretch" Margin="0,12,0,0" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Morning Pages" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <TextBlock Text="Esta semana" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" Margin="0,4,0,0" />
                    <StackPanel x:Name="StreakPanel" Orientation="Horizontal" Margin="0,8,0,0" />
                    <Button x:Name="ToggleMpButton" Click="ToggleMp_Click" HorizontalAlignment="Stretch" Margin="0,12,0,0" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource NudgeBorderStyle}">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Afirmação de hoje" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" HorizontalAlignment="Center" />
                    <TextBlock x:Name="AffirmationText" Style="{StaticResource BodyTextBlockStyle}" FontWeight="SemiBold" TextWrapping="Wrap" TextAlignment="Center" Margin="0,4,0,0" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Artist Date dessa semana" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <TextBlock x:Name="ArtistDateStatusText" Style="{StaticResource BodyTextBlockStyle}" TextWrapping="Wrap" Opacity="0.85" Margin="0,4,0,0" />
                    <Button x:Name="OpenArtistDateButton" Click="OpenArtistDate_Click" HorizontalAlignment="Stretch" Margin="0,12,0,0" />
                </StackPanel>
            </Border>

            <Border x:Name="CheckinNudgeCard" Style="{StaticResource NudgeBorderStyle}">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Prefere revisar a semana agora?" Style="{StaticResource BodyTextBlockStyle}" Opacity="0.85" HorizontalAlignment="Center" />
                    <Button Content="Ir para o check-in" Click="OpenCheckin_Click" HorizontalAlignment="Stretch" Margin="0,8,0,0" />
                </StackPanel>
            </Border>

            <Border x:Name="RoadRulesNudgeCard" Visibility="Collapsed" Style="{StaticResource NudgeBorderStyle}">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Faz uns dias que você não passa por aqui." Style="{StaticResource BodyTextBlockStyle}" Opacity="0.85" HorizontalAlignment="Center" />
                    <Button Content="Já revisou as Regras da Estrada?" Click="OpenRoadRules_Click" HorizontalAlignment="Stretch" Margin="0,8,0,0" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
