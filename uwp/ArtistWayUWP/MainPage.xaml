<Page
    x:Class="ArtistWayUWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArtistWayUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <!--
          O Button padrão do UWP tem um chrome arredondado/recuado no
          estado Pressed/PointerOver — fica parecendo um botão de app
          Android, não uma linha de lista nativa do Windows. Esse template
          simplificado faz o destaque preencher o quadradinho inteiro
          (mesmas brushes que ListViewItem usa pra seleção), sem cantos
          arredondados nem margem sobrando.
        -->
        <Style x:Key="TabButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid x:Name="RootGrid" Background="Transparent">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource SystemControlBackgroundListLowBrush}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource SystemControlBackgroundListMediumBrush}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Disabled" />
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <ContentPresenter x:Name="ContentPresenter"
                                              Content="{TemplateBinding Content}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Frame x:Name="ContentFrame" x:FieldModifier="public" Grid.Row="0" />

        <Border x:Name="ErrorPanel"
                Visibility="Collapsed"
                Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                Padding="20"
                Grid.Row="0">
            <TextBlock x:Name="ErrorText"
                       TextWrapping="Wrap"
                       Style="{StaticResource BodyTextBlockStyle}" />
        </Border>

        <Grid x:Name="TabBar"
              Grid.Row="1"
              Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
              Padding="0,6,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button x:Name="TabHome" Grid.Column="0" Tag="Home" Click="TabButton_Click"
                    Style="{StaticResource TabButtonStyle}">
                <StackPanel HorizontalAlignment="Center">
                    <SymbolIcon x:Name="TabHomeIcon" Symbol="Home" HorizontalAlignment="Center" />
                    <TextBlock x:Name="TabHomeLabel" Text="Início" Style="{StaticResource CaptionTextBlockStyle}" HorizontalAlignment="Center" />
                </StackPanel>
            </Button>

            <Button x:Name="TabProgress" Grid.Column="1" Tag="Progress" Click="TabButton_Click"
                    Style="{StaticResource TabButtonStyle}">
                <StackPanel HorizontalAlignment="Center">
                    <SymbolIcon x:Name="TabProgressIcon" Symbol="List" HorizontalAlignment="Center" />
                    <TextBlock x:Name="TabProgressLabel" Text="Jornada" Style="{StaticResource CaptionTextBlockStyle}" HorizontalAlignment="Center" />
                </StackPanel>
            </Button>

            <Button x:Name="TabArtistDate" Grid.Column="2" Tag="ArtistDate" Click="TabButton_Click"
                    Style="{StaticResource TabButtonStyle}">
                <StackPanel HorizontalAlignment="Center">
                    <SymbolIcon x:Name="TabArtistDateIcon" Symbol="Favorite" HorizontalAlignment="Center" />
                    <TextBlock x:Name="TabArtistDateLabel" Text="Date" Style="{StaticResource CaptionTextBlockStyle}" HorizontalAlignment="Center" />
                </StackPanel>
            </Button>

            <Button x:Name="TabSettings" Grid.Column="3" Tag="Settings" Click="TabButton_Click"
                    Style="{StaticResource TabButtonStyle}">
                <StackPanel HorizontalAlignment="Center">
                    <SymbolIcon x:Name="TabSettingsIcon" Symbol="Setting" HorizontalAlignment="Center" />
                    <TextBlock x:Name="TabSettingsLabel" Text="Ajustes" Style="{StaticResource CaptionTextBlockStyle}" HorizontalAlignment="Center" />
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</Page>
